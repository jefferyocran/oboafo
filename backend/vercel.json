{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "functions": {
    "api/index.py": {
      "maxDuration": 300,
      "memory": 3008,
      "excludeFiles": "{**/scripts/**,**/__pycache__/**,**/*.pyc,**/.venv/**,**/venv/**}"
    }
  },
  "installCommand": "pip install -r requirements.txt && (test -f data/faiss_index/index.faiss && test -f data/faiss_index/metadata.json || python -m app.services.rag --build)"
}
